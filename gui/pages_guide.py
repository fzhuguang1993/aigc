"""
gui/pages_guide.py —— 新手入门：内置图文教程，面向没有编程基础的同事
"""
from PySide6.QtCore import Qt
from PySide6.QtWidgets import QWidget, QVBoxLayout, QHBoxLayout, QListWidget, QTextBrowser

from gui.header import page_header

HTML_STYLE = """"""

# Qt 富文本不支持 <style>/class，统一用下列辅助函数生成可渲染的块
TIP_OPEN = '<table bgcolor="#EAF1FF" cellpadding="10" width="100%"><tr><td>'
WARN_OPEN = '<table bgcolor="#FFF3E8" cellpadding="10" width="100%"><tr><td>'
TIP_CLOSE = '</td></tr></table>'

CHAPTERS = [
    ("🚀 5 分钟快速上手", """
<h1>5 分钟快速上手</h1>
<p>这个软件帮你<b>批量生成 AI 带货短视频</b>。全程不需要懂代码，记住四步就行：</p>
<h3>第 1 步 · 填姓名（只做一次）</h3>
<p>第一次打开会弹「首次配置」：接口地址已由维护人内置在软件里，
<b>只填一个姓名</b>（用于视频文件命名）就能开始。想确认地址通不通，
再去左侧「⚙️ 设置」点「🔌 测试连通性」看 🟢。</p>
<div_class_tip>只有维护机没把地址编进来时（例如公司换了服务），首配窗口才会多出一
个地址输入框，那时才需要自己粘接口地址。</div_class_tip_end>
<h3>第 2 步 · 建产品（只做一次）</h3>
<p>左侧点「🧩 产品中心」→「＋ 新建产品」输入品名 → 在树的右键菜单里<b>添加参考图片</b>。</p>
<div_class_tip>提交任务时，软件会按品名<b>自动带上这里登记的参考图</b>，不用每次手动传。</div_class_tip_end>
<h3>第 3 步 · 导入/新建任务</h3>
<p>「📋 任务中心」→「📁 导入/导出」→ 先「下载导入模板」，往模板里填提示词，再「从 Excel 导入」。
也可以「＋ 新建任务」一条条填。</p>
<h3>第 4 步 · 执行</h3>
<p>点「🔍 扫描新任务」→ 确认「帮你勾选」→ 选时长（2~15 秒）和步数（1~50）→ 点「▶ 执行选中」。</p>
<p>之后<b>什么都不用管</b>：状态列会出现蓝色进度条实时更新，生成完成后视频自动下载到
<code>outputs/</code> 文件夹，「输出文件」列显示文件名。</p>
<ul>
<li>▶ <b>双击输出文件名</b>：弹窗播放（无边框，横竖屏自动适配，可拖可缩放可调倍速）</li>
<li>▶ <b>右键输出文件</b>：快捷定位到文件夹 / 复制视频文件 / 标「可用・不可用」</li>
<li>▶ <b>看完顺手标一下</b>：不能用的标「不可用」（文件名上会留记号），
回列表点「🧹 清理不可用」一次丢进回收站</li>
<li>▶ <b>左下角「📂 输出文件夹」</b>：一键打开所有成品视频的存放目录</li>
</ul>
"""),
    ("📋 任务中心详解", """
<h1>任务中心详解</h1>
<h2>勾选与批量操作</h2>
<ul>
<li>表格<b>第 1 列是复选框</b>，勾选才算选中；<b>翻页后勾选不丢</b>（跨页保留）</li>
<li><b>鼠标按住拖出一个框</b>（橡皮筋）→ 松手就把框到的行全勾上；
勾完高亮自动清掉，<b>以第 1 列的勾为准</b>（框选只加不减，取消请点勾或「清除选择」）</li>
<li>点<b>勾选列的表头</b>＝ 全选/取消全选本页；<code>Ctrl+A</code> 全选本页</li>
<li>「执行选中」只会执行勾选的任务；「执行全部待办」自动挑出所有没跑过的</li>
</ul>
<h2>查看与预览</h2>
<ul>
<li><b>鼠标悬停</b>提示词/口播文案格子 → 弹出保留排版的全文浮层，超长可滚动</li>
<li><b>双击</b>提示词等格子 → 打开编辑窗口；双击输出文件 → 播放视频</li>
<li>点<b>表头</b> → 该列升/降序；时间、运行次数按数值排序</li>
<li><b>右键任务行 →「🧐 口播规范检测」</b>：按该产品的规范卡＋风控政策自动检查禁用词/价格口径/必含话术</li>
</ul>
<h2>▶ 播放器（双击成品视频弹出）</h2>
<ul>
<li><b>无边框</b>：整个窗口就是视频，<b>拿鼠标在画面上按住就能拖走</b>，
右下角有个小三角，按住可以拖大小</li>
<li><b>横竖屏自适应</b>：开窗时按视频真实宽高比算尺寸，<b>横屏视频给横窗、
竖屏视频给窄高窗</b>，画面区尺寸正好等于视频比例，不会左右（或上下）一大块黑边；
默认就撑到接近全屏大小，不用手动拖大</li>
<li><b>进度条</b>：<b>拖着走，点哪儿就跳到哪儿</b>（审片时想直接看后半段，点一下就行）</li>
<li><b>按秒数快退/快进</b>：控制条上是「<code>－</code>　<code>5秒▼</code>　<code>＋</code>」，
中间那个下拉选 1~5 秒的步长（对口型调成 1 秒），选过的下次开播放器还在</li>
<li><b>倍速</b>：第二行那个 <code>1x</code> 下拉可选 0.5~2 倍速；键盘 <code>,</code> / <code>.</code>
逐档加减，<code>/</code> 回 1 倍速</li>
<li><b>全屏</b>：右上角 <code>⛶</code> 或按 <code>F</code>；退出按 <code>Esc</code></li>
<li>常用快捷键：<code>空格</code> 暂停、<code>←→</code> 按步长退/进、<code>↑↓</code> 音量、
<code>M</code> 静音、<code>L</code> 循环、<code>G</code> 打开所在文件夹</li>
<li><b>👍 可用 / 👎 不可用</b>：第二行右边那两个按钮（再点一次＝取消标记），
标「不可用」会给文件名加上 <code>_不可用</code> 后缀，同事不开软件也看得出哪些别发</li>
</ul>
<h2>🧹 审片与清理不可用</h2>
<ul>
<li>标过的成品在「输出文件」那一格会有图标与颜色：<b>⛔ 不可用</b>（红底）、
<b>✅ 可用</b>（绿字），不用自己去找文件名后缀；导出 Excel 时也另有一列「审核」</li>
<li>工具栏「<b>🧹 清理不可用</b>」：把标了不可用的成品<b>一次移到【回收站】</b>，
确认框里会先报清楚几个、共多少 MB、都是哪几个文件</li>
<li>只删视频文件与对应标记，<b>不删任务与执行记录</b>：那次执行确实发生过，
成功率与平均生成时长不该因为事后删片而变</li>
<li>同事在资源管理器里<b>自己改的名也认</b>（只要文件名带「不可用」），一样会被扫出来</li>
</ul>
<ul>
<li><b>右键输出文件那一格</b>：▶ 播放、<b>📍 快捷定位</b>（打开成品所在文件夹并选中这个文件）、
<b>📋 复制视频文件</b>（拷进系统剪切板，出去直接 Ctrl+V 粘到微信/飞书/资源管理器）、
以及 👍/👎 标记</li>
</ul>
<h2>字段管理 / 筛选 / 分页</h2>
<ul>
<li>「⚟ 字段管理」：勾选控制显示哪些列；<b>直接拖动表头</b>就能调整列顺序（第 1 列锁定不可拖）</li>
<li><b>每一个字段都能放进字段管理</b>（包括「取消」「job_id」「URL」这三列技术字段）。
第一次用默认收起 <code>job_id</code>/<code>URL</code>，找维护人排障时再勾出来。</li>
<li>列的<b>顺序和显隐会记住</b>，下次开软件还是你摆好的样子，不用重摆</li>
<li><b>「备注」列是自己写给自己看的记号</b>：右键任务→「🏷 写备注」，或<b>双击备注格子</b>就地改。
先勾选一批再写＝<b>同一句话批量盖上</b>；留空＝清除。</li>
<li>备注<b>不影响提交、不影响重跑判定</b>，纯管理用；也能在搜索框里搜到</li>
<li><b>筛选下拉</b>：品名 / 状态 / 备注 三个框，候选值从库里的实际内容自动汇总，
与搜索框是<b>“同时满足”</b>的关系（例如：品名＝诺特兰德益生菌 + 状态＝失败）。
状态框里的「待执行」＝从没跑过的；「失败」含超时。点「✖ 清除筛选」全放开</li>
<li>搜索框：输入品名/编号/脚本/提示词/口播文案/备注关键词，回车过滤</li>
<li>「<b>🔁 查找/替换</b>」像 Excel 一样：<b>默认收起就是查找</b>，输入关键词点「查找全部」，
把命中的任务<b>自动勾选并置顶</b>（不改文字），直接「执行选中」就能只跑这批</li>
<li>要找的词同时要批量改掉？点弹窗左下角的「<b>替换 »</b>」展开替换行，输「替换为」后点
「<b>全部替换</b>」；改完同样<b>自动勾选并置顶那批命中的任务</b>，任务多了也不怕在列表里找不到</li>
<li>「📁 从 Excel 导入」/「🔍 扫描新任务」同样会把刚进来的那批<b>勾选并置顶聚集</b></li>
<li>「按日期筛选」：勾选后选起止日期，按任务更新时间过滤</li>
<li>右下角可切 20/50/100 条每页或全部显示；<b>置顶聚集装不下时会自动提升每页条数</b></li>
</ul>
<div_class_tip>状态颜色：<font color="#3370FF">蓝＝排队/提交</font>、
<font color="#FF8D19">橙＝进行中（带进度条）</font>、<font color="#00A870">绿＝完成</font>、
<font color="#F54A45">红＝失败（可重新执行）</font>、<font color="#8F959E">灰＝取消</font></div_class_tip_end>
<h2>时长 / 步数是工具栏的「全局开关」</h2>
<p>工具栏的<b>时长</b>、<b>步数</b>、<b>KOL</b> 是<b>这一批任务共用</b>的参数，
改完马上对下一次「执行选中」生效，不用逐条任务改。
表格里那个「<b>时长</b>」列是<b>上一次提交时用的值</b>，只能回看，<b>改不了也不起作用</b>。</p>
<p>上一次选的值<b>下次开软件还会在</b>（存在运行目录的 <code>ui_state.json</code>），
不会被重置成 5 秒 / 8 步。</p>
"""),
    ("🧩 产品中心", """
<h1>产品中心</h1>
<p>所有素材在这里统一管理，左侧是<b>树状目录</b>：</p>
<p><code>📦 产品 → 品名 → 🖼 参考图片 / 🎬 参考视频 / 🔊 音频 → 具体文件</code></p>
<ul>
<li><b>双击树里的文件</b> → 弹窗预览（图片缩放显示，视频/音频直接播放）</li>
<li><b>拖拽上传</b>：从资源管理器把文件（夹）直接拖到树的产品节点或右侧素材区，
按扩展名自动归到图片/视频/音频类</li>
<li><b>右键</b>品名 → 添加各类素材；右键文件 → 打开 / 打开所在文件夹 / 移除登记</li>
<li>添加的素材会自动复制到 <code>material/products/品名/</code> 下集中保管</li>
</ul>
<h2>📐 规范卡</h2>
<p>选中产品后点右上角「📐 规范卡」，把这个品的<b>卖什么套餐、价格口径、活动口径、
核心卖点、必含话术、禁用词</b>等填进去保存。之后在任务中心右键「🧐 口播规范检测」
就会按这张卡逐项核对口播脚本（详见第七章）。</p>
<div_class_warn>「移除登记」「删除条目」只清软件里的记录，<b>不会删除磁盘上的素材文件</b>，
想彻底清理请自己到文件夹里删，避免误删。</div_class_tip_end>
<h3>KOL 形象</h3>
<p>「＋ 新建 KOL」输入明星名字，给他登记一张<b>形象图</b>。之后任务中心上方的 KOL 下拉
就会出现这个名字，执行时自动把形象图作为参考图一起提交。</p>
"""),
    ("📊 数据看板", """
<h1>数据看板</h1>
<ul>
<li>右上角切换<b>时间范围</b>：今日 / 近 7 / 14 / 30 / 90 天 / <b>全部（累计）</b></li>
<li>选中的范围<b>下次开软件还会在</b>，不会被重置</li>
<li>七张指标卡显示执行量、成功、成功率、失败、取消、正在运行、<b>平均生成时长</b>，
小字是<b>与上一个等长周期的环比</b>（▲绿＝变好，▼红＝变差）</li>
<li>「平均生成时长」只算视频真正在跑的那段，<b>不含排队</b>（鼠标停上去能看到平均排队多久）</li>
<li>选「<b>全部（累计）</b>」是从第一条记录累计到今天、<b>永不清零</b>，
环比位置显示「累计至今」（没有等长的上一周期可比）</li>
<li>下方三张图：每日执行趋势（堆叠柱）、状态占比（环形）、各线路执行分布；
累计口径下只画<b>有记录的那几天</b>（最多最近 60 天）</li>
<li>线路分布标注意思是：<code>共15 · 成12</code> ＝ 该线路执行 15 条、成功 12 条</li>
</ul>
<h2>一键汇报</h2>
<p>点「📋 复制进度汇报」→ 填入汇报人姓名（默认用设置里的姓名）→
<b>今日截至目前 vs 昨日</b>的汇报文本自动进入剪切板，直接在微信/飞书群里粘贴：</p>
<div_class_tip>📊 AIGC 数据汇报<br>姓名：张三<br>日期：2026年9月21日（截至目前 09:30）<br><br>【总体】<br>　总运行 8 次 ｜ 成功 6 次 ｜ 成功率 75%<br>　环比昨日：运行 ▲33%，成功 ▲50%<br><br>【分产品】<br>　诺特兰德益生菌：跑 5 次，成功 4 次<br>　蛋白粉：跑 3 次，成功 2 次<br><br>【视频时长】<br>　10 秒以上：5 条，占比 62% ｜ 环比昨日 ▲25%<br>　10 秒以下：3 条，占比 38% ｜ 环比昨日 ▲50%</div_class_tip_end>
<p>汇报固定按「今天到目前为止、和昨日同期比」的口径，<b>不受右上角时间范围影响</b>；
视频时长取任务登记的「时长」列，时长占比按有明确时长的条数计算。</p>
"""),
    ("🕘 记录 / 📡 负载 / ⚙️ 设置", """
<h1>执行记录 · 线路负载 · 设置</h1>
<h2>🕘 执行记录</h2>
<p>每一次提交尝试都留一行：谁在什么时间、用哪条线路、成败、输出在哪。
支持搜索、日期筛选、排序、字段管理（同任务中心）。
<b>勾选第 1 列后点「导出选中」</b>可把这批记录导成 Excel；双击格子看完整信息。</p>
<h2>📡 线路负载</h2>
<p>看每条接口线路的实时情况：并发上限、正在跑几条、剩余槽位、健康状态。</p>
<div_class_tip>绿灯是<b>测出来的</b>：软件一启动就先逐条真实探活一轮（不再“先睡 30 秒”），
没测完之前侧栏写「服务：检测中…」、本页状态列写「⚪ 待检测」，
<b>不会拿默认值骗你一眼绿灯</b>。想立刻重测：<b>点一下左侧栏那排灯</b>即可。</div_class_tip_end>
<div_class_tip>提交时的排队逻辑：一条线路的在途任务碰到「并发上限」就<b>不再往它灌</b>，
只提交给还有空位的线路；几条线全满时新任务会等前面跑完再发（并发 1 就是一条线
同时只跑一个）；某条线路<b>连续 3 轮探活都失败</b>（每轮重试 2 次）才会被标红并
自动换别的线路，下次重试成功会自动恢复 🟢——偶尔网络闪一下不会误杀。</div_class_tip_end>
<p>「🔄 查询云端负载」向每条线路询问云端队列里还有几条没跑完（<b>包含同事提交的</b>），
结果缓存约 5 秒。显示 <b>「N ⚠仅本机」</b>表示这条线路的接口问不通，
此时只能看到自己这台电脑的任务，选线会偏——本页默认不展示接口地址（凭证不露出），
该找维护人检查线路或重发一份配置包导入。</p>
<div_class_tip>为什么「几条线一样空」时要<b>随机挑一条</b>：大家用的是同一份配置、
线路顺序一模一样，如果都“选第一条最空的”，结果就是几个人同时把同一条线灌爆、
其余线全程空转。随机散开 + 两条任务之间错开秒数，已经能避掉绝大部分撞车。</div_class_tip_end>
<h2>⚙️ 设置</h2>
<p><b>线路与各接口地址默认是藏起来的</b>（接口地址本身就是访问凭证，不该摆在界面上）：
日常使用只需要填姓名、认命名规则、用「📦 配置迁移」拿包 —— 换线路找维护人要
线路包点「<b>📥 导入线路</b>」，换机器要整套配置就点「<b>📥 一键导入配置</b>」，
全程不用手敲任何地址。
维护人需要亲自改线路时，在设置页按维护口令解锁，才会出现下面这些编辑区。</p>
<p>（维护人）改接口地址、账号名、并发数。一个地址＝一个账号，可以多条线路并行跑量。
<b>行高已抬到 45px</b>，长地址双击编辑不再被裁掉半截。每行末尾有个
「<b>🌐 打开</b>」按钮：用默认浏览器打开这条线路（自动去掉地址末尾的 <code>/api/v1</code>），
改过地址再点就是新地址，删行后按钮也不会认错行，想确认服务在不在线点一下就行。
<b>线路多了不想一个个点</b>：多选几行再点下方的「<b>🌐 打开选中</b>」，一次把这几条全开成
浏览器标签（同一个地址只开一个，超过 8 条会先问一句）；<b>一行都没选</b>时它会先问
「要打开表里全部 N 条吗」，答否就一个标签都不弹。
「🌐 打开选中」跟「🗑 删除所选行」用的是同一个选中口径。
「测试连通性」在后台执行，不会卡界面。<b>保存后需要重启软件生效</b>。</p>
<p>要删好几条：鼠标<b>按住拖选（或 Ctrl/Shift 点选）多行</b>→ 点「🗑 删除所选行」，
<b>只弹一次确认就全删</b>（不是一行确认一次）。删完仍需点「💾 保存设置」才写盘。</p>
<p>（维护人）「<b>📋 复制线路</b>」把当前线路列表复制成一段文本，适合临时发给信得过的同事；
正经分发还是建议用加密的<b>配置包</b>（见下一节），至少文件路上被人拿到也读不出地址。</p>
<h2>📦 配置迁移：导出配置包 / 一键导入配置</h2>
<p>换新电脑、或者给同事一次性搬全套配置：点「<b>📤 导出配置包</b>」，把
<b>线路与各接口地址/key、SMB 共享盘、溯源 MySQL 账密、成品命名规则、
任务界面字段（列顺序/显隐）、产品与商品规范卡（连产品图片一起搬）、风控政策</b>
打成一个 <code>.aigccfg</code> 文件（默认存桌面）；
对方在设置页点「<b>📥 一键导入配置</b>」选中这个文件就行，<b>不用输任何口令</b>（软件内置）。</p>
<p>导入前会列出「将覆盖哪些东西」让你确认：配置文件旧值备份成
<code>*.bak-时间戳</code>；<b>产品/风控表整表替换，任务列表不受影响</b>；
包里多余的条目（对方软件版本旧）会自动跳过，不会导入失败。</p>
<p>包是<b>加密</b>的，外人拿到文件也看不到接口地址和 key；但里面含真凭证和业务数据，
仍要<b>只发给信得过的同事</b>。导入后<b>重启软件全部生效</b>。</p>
<h2>🔗 线路天天换？用线路小包，别动整套配置</h2>
<p>接口地址每天变（服务搬家/换端口）时，不用每次发整套配置包：维护人改好线路后点
「<b>🔗 导出线路</b>」，只把<b>线路列表</b>（名称+地址+并发）加密成一个小小的
<code>.aigcline</code> 文件（默认存桌面、文件名自带日期，如 <code>线路_0924.aigcline</code>），
发群里；同事点「<b>📥 导入线路</b>」选它就行——<b>只换线路，其它配置（命名规则、
界面字段、SMB、产品…）一个字不动</b>，重启后新线路生效，正在跑的任务不受影响。</p>
<p>两个入口拿错文件不会误事：线路包里塞整包（或反过来）会当场提示让你换入口；
确认框只列线路<b>名字</b>不显示地址，地址全程是密文。</p>
<h2>🏷 成品命名规则（这一项保存即生效，不用重启）</h2>
<p>姓名下面那一行会写出当前规则与一个例子。点「<b>🏷 自定义规则…</b>」：
左边是没轮上的<b>可用字段</b>（编号/品名/日期/序号/姓名/完整日期/时刻/线路/备注/任务ID/作业号），
双击或点「添加」放进右边，<b>在右边拖一拖（或用上移/下移）就能换拼接顺序</b>，
再选个字段之间的分隔符，<b>下面的示例文件名实时跟着变</b>。</p>
<p>点「💾 保存并生效」就写进 <code>config.json</code> 并<b>立刻生效</b>（其它设置填完
仍需重启）；只影响<b>之后新下载</b>的成品，已经生成的不会回头改名。
不排「序号」时同一天多条撞名会在尾巴挂 <code>(2)</code>，不会互相覆盖（弹窗里会提醒）。</p>
"""),
    ("❓ 常见问题", """
<h1>常见问题 FAQ</h1>
<h3>Q1：点了执行没反应 / 提示没有可执行任务？</h3>
<p>确认：① 任务<b>提示词不为空</b>；② 已经<b>勾选</b>（第 1 列打勾）；③ 设置里接口地址测试是 🟢。</p>
<h3>Q2：日志出现「acc1 标记为不可用」是不是坏了？</h3>
<p>不是。这是<b>自动保护</b>：接口连不上时自动切换其他线路，接口恢复后会自动回到 🟢，无需操作。
而且现在很难被“误杀”：一轮检测里会重试一次，连续 3 轮全败才标红。</p>
<h3>Q3：GUI 和黑色命令行窗口能同时开吗？</h3>
<p>可以。数据统一存在 <code>data/aigc.db</code>，两边读写同一份数据，不会互相覆盖。</p>
<h3>Q4：视频保存在哪？</h3>
<p>项目目录 <code>outputs/</code>，文件名含任务信息。任务中心双击即可播放，右键可打开所在文件夹。</p>
<h3>Q5：提示词想批量改一个词怎么办？任务那么多怎么只跑改过的？</h3>
<p>「🔁 查找/替换」：默认就是查找（不改文字），点「替换 »」展开后才批量改写。可先勾选要改的任务
（或直接改全部），输入查找/替换内容即可。改完这批任务会<b>自动勾选并排到列表最前面</b>
（超出当前每页条数时自动提升），点「▶ 执行选中」就只重跑它们。</p>
<h3>Q5b：导入/上传了一批旧提示词，在列表里翻不到？</h3>
<p>不用翻：不管这批提示词是<b>新建</b>还是<b>库里已经有了</b>（按提示词去重被跳过），
导入后都会自动<b>勾选 + 置顶聚集</b>在前几行，直接执行即可。
点「✖ 清除筛选」可回到默认的最新在前顺序。</p>
<h3>Q6：之前硬编码的参考图/KOL 还生效吗？</h3>
<p>生效。产品里<b>没登记参考图</b>时自动回退旧的全局配置图；KOL 没在产品中心建时，
回退 <code>material/KOL/名字.png</code>。逐步迁移到产品中心即可，不用一次改完。</p>
<h3>Q7：软件从哪打开？</h3>
<p>开发环境：<code>python desktop.py</code>；打包后双击 <code>AIGC视频助手-GUI.exe</code>。
界面内「打开命令行窗口」按钮可随时切到黑窗口模式。</p>
<h3>Q8：点「执行」后有些任务在提交阶段卡挺久，是不是卡死了？</h3>
<p>不是，这是在<b>等线路空位</b>：几条线全部排满时，新任务会等前面跑完再提交，
避免一条线被灌二十多条、别的线全程空转。日志里会写「全部线路已排满（acc1:1 acc2:1 …），
等空位…」。真想让一条线同时跑多个，去「⚙️ 设置」把该线路的<b>并发数调大</b>
（前提是这个实例确实能同时跑多个），重启后生效。多人同时跑时提交会比单人慢，
这正常——共用的就那几条线。</p>
<h3>Q9：改了时长/步数再执行，跑出来还是上次的样子？</h3>
<p>时长和步数<b>只在工具栏那两个下拉框里改</b>（表格里那列「时长」只是上次提交的留痕，改不动）。</p>
<p>「▶ 执行选中」后，右下角提示（以及每条提交进度）都会写明<b>「本次：10秒 / 20 步」</b>；
遇到需要确认的强制重跑，确认弹窗里也会带上这组参数。以这里显示的数值为准。
看到参数是对的，但成品还是旧的，说明<b>这次提交根本没成功</b>，看下一条。</p>
<h3>Q10：已取消 / 已完成的任务，改完参数重新执行却没动静？</h3>
<p>看有没有<b>弹出「部分任务未提交成功」的告警窗</b>。没提交上去的任务，
列表里显示的还是<b>上一次跑出来的视频和参数</b>，看上去就像「改了没生效」。</p>
<p>告警窗里会写清楚原因（比如云端拒绝了某个参数、接口连不通），修好后
重新勾选再点「▶ 执行选中」即可。已取消的任务<b>不需要先恢复</b>，直接执行就会重新提交。</p>
<h3>Q11：执行时弹「云端不接受参数 parameters.xxx」？</h3>
<p>是本软件发出的参数名/取值不合云端接口，<b>不是你的提示词写错了</b>。
软件会自动去掉被拒的那个参数在同一条线重试（日志会写「该参数本次不生效」），
把弹窗里的字段名截图发给维护人即可，其余参数照常生效。</p>
<h3>Q12：关了软件，数据统计会不会又从零开始？</h3>
<p>不会。统计一直是从数据库<b>现算</b>的，记录存 <code>data/aigc.db</code>。
以前如果看到“归零”，是因为<b>换了一个目录打开 exe</b>——旧版本把数据存在
“启动时所在的那个目录”，于是新建了一份空库。现在已锁定为<b>exe 自己所在的目录</b>，
数据库/成品/日志永远跟着软件走。</p>
<p>想看历年累计（永不清零的口径），把看板右上角切到「<b>全部（累计）</b>」。</p>
<h3>Q13：素材提取的接口地址怎么没输入框了？</h3>
<p>地址本身就是服务方的凭证，<b>默认不摆在使用界面上</b>（也不随代码仓库分发）。
普通同事只需要填自己的 <b>UID / Key</b>，点「保存」即可。</p>
<p>那行显示「✅ 已配置」就是地址已就位；显示「⚠ 未配置」时——
<b>维护人在本页键盘上直接盲输维护口令（屏幕上不显示、不输到任何框），
会自动唤出一个密文的接口地址输入框</b>，粘上地址点「应用地址」即可，无需重新打包。
<b>保存后输入框会自动收起</b>，下次再改还得重新盲输口令。</p>
<div_class_tip>口令属于“防误触 / 不主动暴露”，不是强加密；要真正不外泄，
靠打包时把地址写进仅维护机持有的 <code>config_local.py</code>。</div_class_tip_end>
<h3>Q14：侧栏那排灯，⚪🟢🟡🔴 各是什么意思？</h3>
<p>刚打开时写的是「服务：检测中…」，<b>那不是结果</b>；软件启动就先逐条真实探活，
几秒后才会换灯。看着灯不对、或刚改完地址想立刻确认，
<b>点一下那排灯</b>就是手动重测（后台跑，不卡界面）。</p>
<p>四档：<b>🟢</b> 探活接口正常应答；<b>🟡</b> 服务有话回但那个探活路径云端还没实现
（典型是地址本身能打开、页面却写着「页面未找到」），<b>这是在线的，照常参与跑批</b>，
不必去删线路；<b>🔴</b> 真的连不上（拒连/超时/网关报错），才会被换线避开。</p>
<h3>Q15：昨天提取的口播文案，今天怎么在样本库里找不到了？</h3>
<p>样本库（<code>material/素材提取/文案样本库.csv</code>）<b>只追加、从不覆写</b>：
提取到一条文案就多一行，同一个链接重复提取也会各自留一行（靠「提取时间」区分先后），
第一行一定是表头 <code>提取时间,平台,视频标题,原文链接,文案</code>。</p>
<p>两条使用约定：<b>①</b> 样本库只有一份，<b>不跟着「保存到」跑</b>——换了输出目录，
视频存到新目录，文案行仍追加进上面那个文件（日志每次都会报全路径）；
<b>②</b> 提取时先关掉正打开着这个 CSV 的 Excel/WPS 窗口，否则写不进去（日志会给一条
写入失败提醒，不影响视频下载）；更<u>不要</u>在 Excel 里改完旧副本直接保存，那才会把新行顶掉。</p>
<h3>Q16：多选任务强制重跑，怎么全挤在一条线路上？</h3>
<p>先看「📡 线路负载」页的状态列。旧版本在这里有个坑：云端没实现探活接口时，
每条线路都被当成故障标红，而“全红”当时的兜底是<b>只挑一条还能用的线</b>，
于是整批任务全灌到一条线上。现在改成：全红也在<b>失败次数最少的那几条之间平摊</b>，
并额外提醒一句去核对地址；<b>🟡 黄灯根本不再算故障</b>。</p>
<p>怎么看本批到底跑在哪些线上：提交结束后，右下角提示（和命令行输出）会带一句
<b>「本批 10 条分布：acc1×4 acc2×3 acc3×3」</b>。只用上了一条而别的线还空着时，
这句后面会跟一个 <b>⚠</b> 说明原因（其余线路探活未通过或已排满）。<b>并发数=1</b>
的线路一次只吃一条，排满后要等前一条跑完，这不是 bug，想让一条线同时多跑
就去设置里调大该线路的并发数（前提是这个实例确实能并发）。</p>
<h3>Q17：任务还在跑，我又点了一次「执行选中」，会不会跑两份？</h3>
<p>会先问你一句。弹窗里写清「另有 N 个任务（共 M 个执行）还在云端排队/生成中」，
并给三个选择（只有这批里确实有在跑的才出现）：</p>
<ul>
<li><b>⏹ 取消并重跑</b>（默认项，回车就是它）：先给在跑的那几份发取消请求，再提交新的</li>
<li><b>🔁 直接重跑</b>：旧的那份继续跑，同一个任务两份并行、各出一个成品
（就想留两份对比、或旧的快跑完了不想作废，选这个）</li>
<li><b>跳过</b>：一份都不提交，在跑的保持原样</li>
</ul>
<p>默认给「取消」是因为共用的就那几条线，双份等于白占一次机会。
只有<b>已完成</b>的任务才会照常问「强制重跑」，不多半句。两个已知边界：
<b>重启软件后</b>，上一轮跑到一半的任务已脱离本机跟踪，不会再弹这个提醒
（去「📡 线路负载」看云端真实队列，或用工具栏「⏹ 取消选中」）；
黑色命令行窗口的 <code>run</code> 是排障入口，不拦也不弹确认，但会打印一行
「⚠ 任务 N 还有 X 个执行没跑完，这次会再提交一份」。</p>
<h3>Q18：任务表里的「生成用时」和「排队」两个数是怎么分开的？</h3>
<p>同一条线一次只跑一个视频，批量提交十条时，后面的九条都在云端等前面跑完。
所以一个数拆成两个看：</p>
<ul>
<li><b>生成用时</b>：云端开始跑 → 出片，才是“一条视频要多久”</li>
<li><b>排队</b>：提交 → 云端开始跑，你灌得越满这段越长（不是机器变慢）</li>
</ul>
<p>两者取自云端回报的时间戳，不是本地估的；鼠标停在格子上会写清
「排队 X + 生成 Y = 提交到完成 Z」。看板的「平均生成时长」也只平均生成那一段。</p>
<p><b>改动之前的老记录</b>只存了一个含排队的总时长：任务表会拿它充数并在鼠标提示里
写明“早期记录没拆出排队”，执行记录里「生成」列则是「—」。要把老数据补上真实拆分，
在仓库根目录跑一句（先看看不改库）：</p>
<div_class_tip>先看看不改库：<code>python backfill_run_gen_sec.py --dry-run</code><br>
确认无误再真写：<code>python backfill_run_gen_sec.py</code></div_class_tip_end>
<p>它按 <code>job_id</code> 回云端查一次时间戳，已经有拆分的不会覆盖，重复跑安全；
云端已清掉的历史作业查不到，跳过并计数。</p>
<h3>Q19：弹出的播放器不能全屏、不能调倍速，黑边还很大？</h3>
<p>这几样都已经做了（双击「输出文件」那一格试新版的窗口）：</p>
<ul>
<li><b>黑边大</b>是因为窗口以前是固定方形：现在开窗时先读视频真实宽高比，
<b>横屏视频给横窗、竖屏视频给窄高窗</b>，而画面区尺寸正好等于视频比例，
所以四周不该再有一大块黑；量不到视频尺寸时（个别文件不写分辨率）
也会先按半屏开，不会缩成一个小方块</li>
<li><b>默认就撑到接近全屏</b>（可用区的九成），不再需要自己拖大；
想更大按 <code>F</code> 或点 <code>⛶</code> 全屏，退出 <code>Esc</code></li>
<li><b>进度条能拖也能点</b>：点哪儿跳到哪儿；旁边的 <code>－</code> / <code>＋</code>
按中间下拉选定的 1~5 秒步长快退快进（旧版写死的 ±5 秒已去掉）</li>
<li><b>无边框</b>后拿不到系统的标题栏，所以<b>在画面上按住鼠标拖就能走窗</b>，
右下角小三角拖大小</li>
<li><b>倍速</b>在第二行那个 <code>1x</code> 下拉里（0.5~2 倍），也可用 <code>,</code> / <code>.</code> 逐档调</li>
<li>想两条横向对比？播放器不锁界面，<b>再双击另一条就能多开一个</b>，
拖到副屏上一直开着也不占主窗口地方</li>
</ul>
<div_class_warn>如果画面仍不支持倍速/拖动（报“无法播放”或一直黑屏），
是这台电脑的<b>解码后端</b>的问题，不是文件坏了：先试试双击用系统播放器打开
（右键→📍 快捷定位能找到文件），并告诉维护人。</div_class_tip_end>
<h3>Q20：标了「不可用」的视频去哪了？「🧹 清理不可用」会不会删错？</h3>
<p>标「不可用」<b>不会删文件</b>，只做两件事：文件名尾巴加 <code>_不可用</code>（资源管理器里
也看得出），以及库里记一笔让列表变成红底 ⛔。要真删得自己点「<b>🧹 清理不可用</b>」，
而且：<b>先报清单（几个 / 共多少 MB / 前几个文件名）再确认</b>，删是<b>移到【回收站】</b>
不是直接抹掉，反悔了去回收站右键还原、回软件里取消标记即可。</p>
<p>它<b>只动标了不可用的成品文件</b>：任务与执行记录一条不删（不然成功率与平均生成
时长会被事后删片影响），可用/未标记的视频更不会被碰。移不进去（比如文件正被其它
软件占用）会明确列出哪几条失败，<b>不会静默真删</b>。</p>
<h3>Q21：成品文件名能不能按我们组的习惯自己定？</h3>
<p>能。「⚙️ 设置 → 🏷 自定义规则…」里把字段拖排组合、选个分隔符就行（详见第五章）。
<b>改完立刻生效、不用重启</b>，但只影响之后新下载的成品。</p>
<p>老文件想按新规则补名？最好<b>别在资源管理器里手工改</b>：软件存的是当时的完整路径，
手改之后列表里那一格就指不到文件了（双击会报“文件不存在”，得去文件夹里找）。
<b>只有走软件里的 👍/👎 标记</b>才会顺手把库里的路径一起改到新名字上。</p>
"""),
    ("🛡 规范卡与风控", """
<h1>规范卡 × 口播检测 × 风控中心</h1>
<p>三件套配合，把「说错话被封号」的风险在提交前拦住：</p>
<h2>1️⃣ 产品中心 → 📐 规范卡（每个品一张）</h2>
<p>填：卖什么套餐 / 价格口径 / 活动口径 / 核心卖点 / 必含话术 / 禁用词 / 资质备案号。</p>
<h2>2️⃣ 风控中心（跨产品通用政策）</h2>
<p>左侧导航「🛡 风控中心」：维护<b>平台风控</b>（抖音/视频号等平台规则）和
<b>产品风控</b>（只对指定产品生效）。每条政策可挂<b>禁用词</b>，保存后立即参与检测。
软件自带一条「广告法极限词」通用政策作示例。</p>
<h2>3️⃣ 任务中心 → 右键「🧐 口播规范检测」</h2>
<ul>
<li><b>本地规则检测（零配置可用）</b>：命中禁用词＝🔴高风险；口播价格不在价格口径内＝🔴；
缺必含话术＝🟠；活动话术缺兜底＝🔵提示</li>
<li><b>🤖 AI 智能检测（预留接口）</b>：在「⚙️ 设置 → 脚本 AI 检测接口」填入
OpenAI 兼容的地址/Key/模型并启用后，可把脚本+规范卡+风控政策交给大模型做语义级审查
（夸大暗示、场景违规这类字面规则拦不住的）。</li>
</ul>
<div_class_tip>建议流程：新品上架先填规范卡 → 导入任务后先全选右键检测一遍 →
改完提示词再执行，把风控前置。</div_class_tip_end>
"""),
]

TITLES = [t for t, _ in CHAPTERS]
BODIES = dict(CHAPTERS)


class GuidePage(QWidget):
    def __init__(self, parent=None):
        super().__init__(parent)
        lay = QVBoxLayout(self)
        lay.setContentsMargins(24, 12, 24, 12)
        lay.setSpacing(8)
        lay.addWidget(page_header("新手入门", "零基础看图学会全部功能", icon="📖"))

        body = QHBoxLayout()
        body.setSpacing(12)
        self.nav = QListWidget()
        self.nav.addItems(TITLES)
        self.nav.setFixedWidth(190)
        self.nav.currentRowChanged.connect(self._show)
        body.addWidget(self.nav)

        self.view = QTextBrowser()
        self.view.setOpenExternalLinks(True)
        self.view.setStyleSheet("QTextBrowser { background:#FFFFFF; border:1px solid #E5E7EB;"
                                " border-radius:10px; }")
        body.addWidget(self.view, 1)
        lay.addLayout(body, 1)
        self.nav.setCurrentRow(0)

    def _show(self, i):
        html = (BODIES.get(TITLES[i], "")
                .replace("<div_class_tip>", TIP_OPEN)
                .replace("<div_class_warn>", WARN_OPEN)
                .replace("</div_class_tip_end>", TIP_CLOSE))
        self.view.setHtml(html)

    def refresh(self):
        pass
