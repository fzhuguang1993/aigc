"""
gui/pages_guide.py —— 新手入门：内置图文教程，面向没有编程基础的同事
"""
from PySide6.QtCore import Qt
from PySide6.QtWidgets import QWidget, QVBoxLayout, QHBoxLayout, QListWidget, QTextBrowser

from gui.header import page_header

HTML_STYLE = """"""

# Qt 富文本不支持 <style>/class，统一用下列辅助函数生成可渲染的块
TIP_OPEN = '<table bgcolor="#EAF1FF" cellpadding="10" width="100%"><tr><td>'
WARN_OPEN = '<table bgcolor="#FFF3E8" cellpadding="10" width="100%"><tr><td>'
TIP_CLOSE = '</td></tr></table>'

CHAPTERS = [
    ("🚀 5 分钟快速上手", """
<h1>5 分钟快速上手</h1>
<p>这个软件帮你<b>批量生成 AI 带货短视频</b>。全程不需要懂代码，记住四步就行：</p>
<h3>第 1 步 · 填接口（只做一次）</h3>
<p>左侧点「⚙️ 设置」→ 填入你的 API 服务地址 → 点「🔌 测试连通性」看到 🟢 → 「💾 保存设置」。</p>
<h3>第 2 步 · 建产品（只做一次）</h3>
<p>左侧点「🧩 产品中心」→「＋ 新建产品」输入品名 → 在树的右键菜单里<b>添加参考图片</b>。</p>
<div_class_tip>提交任务时，软件会按品名<b>自动带上这里登记的参考图</b>，不用每次手动传。</div_class_tip_end>
<h3>第 3 步 · 导入/新建任务</h3>
<p>「📋 任务中心」→「📁 导入/导出」→ 先「下载导入模板」，往模板里填提示词，再「从 Excel 导入」。
也可以「＋ 新建任务」一条条填。</p>
<h3>第 4 步 · 执行</h3>
<p>点「🔍 扫描新任务」→ 确认「帮你勾选」→ 选时长（2~15 秒）和步数（1~50）→ 点「▶ 执行选中」。</p>
<p>之后<b>什么都不用管</b>：状态列会出现蓝色进度条实时更新，生成完成后视频自动下载到
<code>outputs/</code> 文件夹，「输出文件」列显示文件名。</p>
<ul>
<li>▶ <b>双击输出文件名</b>：直接弹窗播放视频</li>
<li>▶ <b>右键输出文件</b>：打开视频存放的文件夹</li>
<li>▶ <b>左下角「📂 输出文件夹」</b>：一键打开所有成品视频的存放目录</li>
</ul>
"""),
    ("📋 任务中心详解", """
<h1>任务中心详解</h1>
<h2>勾选与批量操作</h2>
<ul>
<li>表格<b>第 1 列是复选框</b>，勾选才算选中；<b>翻页后勾选不丢</b>（跨页保留）</li>
<li>点<b>勾选列的表头</b>＝ 全选/取消全选本页</li>
<li>「执行选中」只会执行勾选的任务；「执行全部待办」自动挑出所有没跑过的</li>
</ul>
<h2>查看与预览</h2>
<ul>
<li><b>鼠标悬停</b>提示词/口播文案格子 → 弹出保留排版的全文浮层，超长可滚动</li>
<li><b>双击</b>提示词等格子 → 打开编辑窗口；双击输出文件 → 播放视频</li>
<li>点<b>表头</b> → 该列升/降序；时间、运行次数按数值排序</li>
<li><b>右键任务行 →「🧐 口播规范检测」</b>：按该产品的规范卡＋风控政策自动检查禁用词/价格口径/必含话术</li>
</ul>
<h2>字段管理 / 筛选 / 分页</h2>
<ul>
<li>「⚟ 字段管理」：勾选控制显示哪些列；<b>直接拖动表头</b>就能调整列顺序（第 1 列锁定不可拖）</li>
<li><b>每一个字段都能放进字段管理</b>（包括「取消」「job_id」「URL」这三列技术字段）。
第一次用默认收起 <code>job_id</code>/<code>URL</code>，找维护人排障时再勾出来。</li>
<li>列的<b>顺序和显隐会记住</b>，下次开软件还是你摆好的样子，不用重摆</li>
<li><b>「备注」列是自己写给自己看的记号</b>：右键任务→「🏷 写备注」，或<b>双击备注格子</b>就地改。
先勾选一批再写＝<b>同一句话批量盖上</b>；留空＝清除。</li>
<li>备注<b>不影响提交、不影响重跑判定</b>，纯管理用；也能在搜索框里搜到</li>
<li><b>筛选下拉</b>：品名 / 状态 / 备注 三个框，候选值从库里的实际内容自动汇总，
与搜索框是<b>“同时满足”</b>的关系（例如：品名＝诺特兰德益生菌 + 状态＝失败）。
状态框里的「待执行」＝从没跑过的；「失败」含超时。点「✖ 清除筛选」全放开</li>
<li>搜索框：输入品名/编号/脚本/提示词/口播文案/备注关键词，回车过滤</li>
<li>「<b>🔁 搜索替换/定位</b>」批量改写提示词：改完会<b>自动勾选并置顶那批命中的任务</b>，
直接「执行选中」即可，任务多了也不怕在列表里找不到</li>
<li>只想把<b>按关键词找出的一批任务</b>批量跑、不改文字？勾上弹窗里的
<b>「只定位勾选，不修改提示词」</b>，同样会勾选＋置顶</li>
<li>「📁 从 Excel 导入」/「🔍 扫描新任务」同样会把刚进来的那批<b>勾选并置顶聚集</b></li>
<li>「按日期筛选」：勾选后选起止日期，按任务更新时间过滤</li>
<li>右下角可切 20/50/100 条每页或全部显示；<b>置顶聚集装不下时会自动提升每页条数</b></li>
</ul>
<div_class_tip>状态颜色：<font color="#3370FF">蓝＝排队/提交</font>、
<font color="#FF8D19">橙＝进行中（带进度条）</font>、<font color="#00A870">绿＝完成</font>、
<font color="#F54A45">红＝失败（可重新执行）</font>、<font color="#8F959E">灰＝取消</font></div_class_tip_end>
<h2>时长 / 步数是工具栏的「全局开关」</h2>
<p>工具栏的<b>时长</b>、<b>步数</b>、<b>KOL</b> 是<b>这一批任务共用</b>的参数，
改完马上对下一次「执行选中」生效，不用逐条任务改。
表格里那个「<b>时长</b>」列是<b>上一次提交时用的值</b>，只能回看，<b>改不了也不起作用</b>。</p>
<p>上一次选的值<b>下次开软件还会在</b>（存在运行目录的 <code>ui_state.json</code>），
不会被重置成 5 秒 / 8 步。</p>
"""),
    ("🧩 产品中心", """
<h1>产品中心</h1>
<p>所有素材在这里统一管理，左侧是<b>树状目录</b>：</p>
<p><code>📦 产品 → 品名 → 🖼 参考图片 / 🎬 参考视频 / 🔊 音频 → 具体文件</code></p>
<ul>
<li><b>双击树里的文件</b> → 弹窗预览（图片缩放显示，视频/音频直接播放）</li>
<li><b>拖拽上传</b>：从资源管理器把文件（夹）直接拖到树的产品节点或右侧素材区，
按扩展名自动归到图片/视频/音频类</li>
<li><b>右键</b>品名 → 添加各类素材；右键文件 → 打开 / 打开所在文件夹 / 移除登记</li>
<li>添加的素材会自动复制到 <code>material/products/品名/</code> 下集中保管</li>
</ul>
<h2>📐 规范卡</h2>
<p>选中产品后点右上角「📐 规范卡」，把这个品的<b>卖什么套餐、价格口径、活动口径、
核心卖点、必含话术、禁用词</b>等填进去保存。之后在任务中心右键「🧐 口播规范检测」
就会按这张卡逐项核对口播脚本（详见第七章）。</p>
<div_class_warn>「移除登记」「删除条目」只清软件里的记录，<b>不会删除磁盘上的素材文件</b>，
想彻底清理请自己到文件夹里删，避免误删。</div_class_tip_end>
<h3>KOL 形象</h3>
<p>「＋ 新建 KOL」输入明星名字，给他登记一张<b>形象图</b>。之后任务中心上方的 KOL 下拉
就会出现这个名字，执行时自动把形象图作为参考图一起提交。</p>
"""),
    ("📊 数据看板", """
<h1>数据看板</h1>
<ul>
<li>右上角切换<b>时间范围</b>：今日 / 近 7 / 14 / 30 / 90 天 / <b>全部（累计）</b></li>
<li>选中的范围<b>下次开软件还会在</b>，不会被重置</li>
<li>六张指标卡显示执行量、成功、成功率、失败、取消、正在运行，
小字是<b>与上一个等长周期的环比</b>（▲绿＝变好，▼红＝变差）</li>
<li>选「<b>全部（累计）</b>」是从第一条记录累计到今天、<b>永不清零</b>，
环比位置显示「累计至今」（没有等长的上一周期可比）</li>
<li>下方三张图：每日执行趋势（堆叠柱）、状态占比（环形）、各线路执行分布；
累计口径下只画<b>有记录的那几天</b>（最多最近 60 天）</li>
<li>线路分布标注意思是：<code>共15 · 成12</code> ＝ 该线路执行 15 条、成功 12 条</li>
</ul>
<h2>一键汇报</h2>
<p>点「📋 复制进度汇报」→ 填入汇报人姓名（默认用设置里的姓名）→
<b>今日截至目前 vs 昨日</b>的汇报文本自动进入剪切板，直接在微信/飞书群里粘贴：</p>
<div_class_tip>📊 AIGC 数据汇报<br>姓名：张三<br>日期：2026年9月21日（截至目前 09:30）<br><br>【总体】<br>　总运行 8 次 ｜ 成功 6 次 ｜ 成功率 75%<br>　环比昨日：运行 ▲33%，成功 ▲50%<br><br>【分产品】<br>　诺特兰德益生菌：跑 5 次，成功 4 次<br>　蛋白粉：跑 3 次，成功 2 次<br><br>【视频时长】<br>　10 秒以上：5 条，占比 62% ｜ 环比昨日 ▲25%<br>　10 秒以下：3 条，占比 38% ｜ 环比昨日 ▲50%</div_class_tip_end>
<p>汇报固定按「今天到目前为止、和昨日同期比」的口径，<b>不受右上角时间范围影响</b>；
视频时长取任务登记的「时长」列，时长占比按有明确时长的条数计算。</p>
"""),
    ("🕘 记录 / 📡 负载 / ⚙️ 设置", """
<h1>执行记录 · 线路负载 · 设置</h1>
<h2>🕘 执行记录</h2>
<p>每一次提交尝试都留一行：谁在什么时间、用哪条线路、成败、输出在哪。
支持搜索、日期筛选、排序、字段管理（同任务中心）。
<b>勾选第 1 列后点「导出选中」</b>可把这批记录导成 Excel；双击格子看完整信息。</p>
<h2>📡 线路负载</h2>
<p>看每条接口线路的实时情况：并发上限、正在跑几条、剩余槽位、健康状态。</p>
<div_class_tip>提交时的排队逻辑：一条线路的在途任务碰到「并发上限」就<b>不再往它灌</b>，
只提交给还有空位的线路；几条线全满时新任务会等前面跑完再发（并发 1 就是一条线
同时只跑一个）；某条线路<b>连续失败 3 次</b>会被标红并自动换别的线路，
下次重试成功会自动恢复 🟢。</div_class_tip_end>
<p>「🔄 查询云端负载」向每条线路询问云端队列里还有几条没跑完（<b>包含同事提交的</b>），
结果缓存约 5 秒。显示 <b>「N ⚠仅本机」</b>表示这条线路的接口问不通，
此时只能看到自己这台电脑的任务，选线会偏，得检查接口地址。</p>
<div_class_tip>为什么「几条线一样空」时要<b>随机挑一条</b>：大家用的是同一份配置、
线路顺序一模一样，如果都“选第一条最空的”，结果就是几个人同时把同一条线灌爆、
其余线全程空转。随机散开 + 两条任务之间错开秒数，已经能避掉绝大部分撞车。</div_class_tip_end>
<h2>⚙️ 设置</h2>
<p>改接口地址、账号名、并发数。一个地址＝一个账号，可以多条线路并行跑量。
「测试连通性」在后台执行，不会卡界面。<b>保存后需要重启软件生效</b>。</p>
<p>「<b>📋 复制线路</b>」把当前线路列表复制成一段文本，微信/飞书发给同事，
对方点「<b>📥 粘贴导入</b>」就一次性导入，不必每台电脑手敲几个地址。
接口地址本身就是访问凭证，<b>只发给内部同事</b>。</p>
"""),
    ("❓ 常见问题", """
<h1>常见问题 FAQ</h1>
<h3>Q1：点了执行没反应 / 提示没有可执行任务？</h3>
<p>确认：① 任务<b>提示词不为空</b>；② 已经<b>勾选</b>（第 1 列打勾）；③ 设置里接口地址测试是 🟢。</p>
<h3>Q2：日志出现「acc1 标记为不可用」是不是坏了？</h3>
<p>不是。这是<b>自动保护</b>：接口连不上时自动切换其他线路，接口恢复后会自动回到 🟢，无需操作。</p>
<h3>Q3：GUI 和黑色命令行窗口能同时开吗？</h3>
<p>可以。数据统一存在 <code>data/aigc.db</code>，两边读写同一份数据，不会互相覆盖。</p>
<h3>Q4：视频保存在哪？</h3>
<p>项目目录 <code>outputs/</code>，文件名含任务信息。任务中心双击即可播放，右键可打开所在文件夹。</p>
<h3>Q5：提示词想批量改一个词怎么办？任务那么多怎么只跑改过的？</h3>
<p>「🔁 搜索替换/定位」：先勾选要改的任务（或直接改全部），输入查找/替换内容即可。
改完这批任务会<b>自动勾选并排到列表最前面</b>（超出当前每页条数时自动提升），
点「▶ 执行选中」就只重跑它们。</p>
<h3>Q5b：导入/上传了一批旧提示词，在列表里翻不到？</h3>
<p>不用翻：不管这批提示词是<b>新建</b>还是<b>库里已经有了</b>（按提示词去重被跳过），
导入后都会自动<b>勾选 + 置顶聚集</b>在前几行，直接执行即可。
点「✖ 清除筛选」可回到完全按编号的默认顺序。</p>
<h3>Q6：之前硬编码的参考图/KOL 还生效吗？</h3>
<p>生效。产品里<b>没登记参考图</b>时自动回退旧的全局配置图；KOL 没在产品中心建时，
回退 <code>material/KOL/名字.png</code>。逐步迁移到产品中心即可，不用一次改完。</p>
<h3>Q7：软件从哪打开？</h3>
<p>开发环境：<code>python desktop.py</code>；打包后双击 <code>AIGC视频助手-GUI.exe</code>。
界面内「打开命令行窗口」按钮可随时切到黑窗口模式。</p>
<h3>Q8：点「执行」后有些任务在提交阶段卡挺久，是不是卡死了？</h3>
<p>不是，这是在<b>等线路空位</b>：几条线全部排满时，新任务会等前面跑完再提交，
避免一条线被灌二十多条、别的线全程空转。日志里会写「全部线路已排满（acc1:1 acc2:1 …），
等空位…」。真想让一条线同时跑多个，去「⚙️ 设置」把该线路的<b>并发数调大</b>
（前提是这个实例确实能同时跑多个），重启后生效。多人同时跑时提交会比单人慢，
这正常——共用的就那几条线。</p>
<h3>Q9：改了时长/步数再执行，跑出来还是上次的样子？</h3>
<p>时长和步数<b>只在工具栏那两个下拉框里改</b>（表格里那列「时长」只是上次提交的留痕，改不动）。</p>
<p>「▶ 执行选中」后，右下角提示（以及每条提交进度）都会写明<b>「本次：10秒 / 20 步」</b>；
遇到需要确认的强制重跑，确认弹窗里也会带上这组参数。以这里显示的数值为准。
看到参数是对的，但成品还是旧的，说明<b>这次提交根本没成功</b>，看下一条。</p>
<h3>Q10：已取消 / 已完成的任务，改完参数重新执行却没动静？</h3>
<p>看有没有<b>弹出「部分任务未提交成功」的告警窗</b>。没提交上去的任务，
列表里显示的还是<b>上一次跑出来的视频和参数</b>，看上去就像「改了没生效」。</p>
<p>告警窗里会写清楚原因（比如云端拒绝了某个参数、接口连不通），修好后
重新勾选再点「▶ 执行选中」即可。已取消的任务<b>不需要先恢复</b>，直接执行就会重新提交。</p>
<h3>Q11：执行时弹「云端不接受参数 parameters.xxx」？</h3>
<p>是本软件发出的参数名/取值不合云端接口，<b>不是你的提示词写错了</b>。
软件会自动去掉被拒的那个参数在同一条线重试（日志会写「该参数本次不生效」），
把弹窗里的字段名截图发给维护人即可，其余参数照常生效。</p>
<h3>Q12：关了软件，数据统计会不会又从零开始？</h3>
<p>不会。统计一直是从数据库<b>现算</b>的，记录存 <code>data/aigc.db</code>。
以前如果看到“归零”，是因为<b>换了一个目录打开 exe</b>——旧版本把数据存在
“启动时所在的那个目录”，于是新建了一份空库。现在已锁定为<b>exe 自己所在的目录</b>，
数据库/成品/日志永远跟着软件走。</p>
<p>想看历年累计（永不清零的口径），把看板右上角切到「<b>全部（累计）</b>」。</p>
<h3>Q13：素材提取的接口地址怎么没输入框了？</h3>
<p>地址本身就是服务方的凭证，<b>默认不摆在使用界面上</b>（也不随代码仓库分发）。
普通同事只需要填自己的 <b>UID / Key</b>，点「保存」即可。</p>
<p>那行显示「✅ 已配置」就是地址已就位；显示「⚠ 未配置」时——
<b>维护人在本页键盘上直接盲输维护口令（屏幕上不显示、不输到任何框），
会自动唤出一个密文的接口地址输入框</b>，粘上地址点「应用地址」即可，无需重新打包。
<b>保存后输入框会自动收起</b>，下次再改还得重新盲输口令。</p>
<div_class_tip>口令属于“防误触 / 不主动暴露”，不是强加密；要真正不外泄，
靠打包时把地址写进仅维护机持有的 <code>config_local.py</code>。</div_class_tip_end>
"""),
    ("🛡 规范卡与风控", """
<h1>规范卡 × 口播检测 × 风控中心</h1>
<p>三件套配合，把「说错话被封号」的风险在提交前拦住：</p>
<h2>1️⃣ 产品中心 → 📐 规范卡（每个品一张）</h2>
<p>填：卖什么套餐 / 价格口径 / 活动口径 / 核心卖点 / 必含话术 / 禁用词 / 资质备案号。</p>
<h2>2️⃣ 风控中心（跨产品通用政策）</h2>
<p>左侧导航「🛡 风控中心」：维护<b>平台风控</b>（抖音/视频号等平台规则）和
<b>产品风控</b>（只对指定产品生效）。每条政策可挂<b>禁用词</b>，保存后立即参与检测。
软件自带一条「广告法极限词」通用政策作示例。</p>
<h2>3️⃣ 任务中心 → 右键「🧐 口播规范检测」</h2>
<ul>
<li><b>本地规则检测（零配置可用）</b>：命中禁用词＝🔴高风险；口播价格不在价格口径内＝🔴；
缺必含话术＝🟠；活动话术缺兜底＝🔵提示</li>
<li><b>🤖 AI 智能检测（预留接口）</b>：在「⚙️ 设置 → 脚本 AI 检测接口」填入
OpenAI 兼容的地址/Key/模型并启用后，可把脚本+规范卡+风控政策交给大模型做语义级审查
（夸大暗示、场景违规这类字面规则拦不住的）。</li>
</ul>
<div_class_tip>建议流程：新品上架先填规范卡 → 导入任务后先全选右键检测一遍 →
改完提示词再执行，把风控前置。</div_class_tip_end>
"""),
]

TITLES = [t for t, _ in CHAPTERS]
BODIES = dict(CHAPTERS)


class GuidePage(QWidget):
    def __init__(self, parent=None):
        super().__init__(parent)
        lay = QVBoxLayout(self)
        lay.setContentsMargins(24, 12, 24, 12)
        lay.setSpacing(8)
        lay.addWidget(page_header("新手入门", "零基础看图学会全部功能", icon="📖"))

        body = QHBoxLayout()
        body.setSpacing(12)
        self.nav = QListWidget()
        self.nav.addItems(TITLES)
        self.nav.setFixedWidth(190)
        self.nav.currentRowChanged.connect(self._show)
        body.addWidget(self.nav)

        self.view = QTextBrowser()
        self.view.setOpenExternalLinks(True)
        self.view.setStyleSheet("QTextBrowser { background:#FFFFFF; border:1px solid #E5E7EB;"
                                " border-radius:10px; }")
        body.addWidget(self.view, 1)
        lay.addLayout(body, 1)
        self.nav.setCurrentRow(0)

    def _show(self, i):
        html = (BODIES.get(TITLES[i], "")
                .replace("<div_class_tip>", TIP_OPEN)
                .replace("<div_class_warn>", WARN_OPEN)
                .replace("</div_class_tip_end>", TIP_CLOSE))
        self.view.setHtml(html)

    def refresh(self):
        pass
